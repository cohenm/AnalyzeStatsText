package com.cohenm.analyzer.io.builder;

/**
 * Typ raportu generowanego przez ReportBuilder.
 * Określa poziom szczegółowości danych w raporcie.
 */
public enum ReportType {
    BASIC,
    FULL,
    FREQUENCY
}
